<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8" />
    <title>{% block title %}TodoApp{% endblock %}</title>
    <!-- Latest compiled and minified CSS -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
    <link rel="stylesheet" href="{{ asset('css/todo.css') }}">
    {% block stylesheets %}{% endblock %}
    <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
</head>

<body>
    <header class="container">
        <h1>TodoApp</h1>
        {% set route = app.request.attributes.get('_route') %}
        <nav>
            <ul class="nav nav-tabs">
                <li{{ route == 'homepage' ? ' class="active"' }}>
                    <a href="{{ path('homepage') }}">A propos</a>
                </li>
                <li{{ route == 'todo_list' ? ' class="active"' }}>
                    <a href="{{ path('todo_list') }}">Todos</a>
                </li>
            </ul>
            {# Notre comparaison a un petit défaut,
            si on est sur la page d'une tâche,
            aucun onglet n'a la classe active
            
            On pourrait améliorer les choses de plein de façons,
            l'une d'elle est de remplacer la deuxième comparaison avec : route != 'homepage'
            #}

            {# Ceci est l'équivalent du code ci-dessus #}
            {# <ul class="nav nav-tabs">
                <li {% if app.request.attributes.get('_route') == 'homepage' %} class="active"{% endif %}>
                    <a href="{{ path('homepage') }}">A propos</a>
                </li>

                <li {% if app.request.attributes.get('_route') == 'todo_list' %} class="active"{% endif %}>
                    <a href="{{ path('todo_list') }}">Todos</a>
                </li>
            </ul> #}
        </nav>
    </header>
    <main class="container">
        {% block body %}{% endblock %}
    </main>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.12.4/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js" integrity="sha384-Tc5IQib027qvyjSMfHjOMaLkfuWVxZxUPnCJA7l2mCWNIpG9mGCD8wGNIcPD7Txa" crossorigin="anonymous"></script>
    {% block javascripts %}{% endblock %}
</body>

</html>